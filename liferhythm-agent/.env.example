# Copy this to .env and fill in your values.
# NEVER commit .env to git.

# Required: Anthropic API key for the intelligence layer
ANTHROPIC_API_KEY=sk-ant-...

# Required: Your Google Sheet ID (from the URL)
LIFERHYTHM_SHEET_ID=your-google-sheet-id-here

# Optional: Override the MCP server location (default: ~/liferhythm-mcp)
# MCP_SERVER_PATH=/path/to/liferhythm-mcp

# Required: Google Sheets credentials for the MCP server
# Option A — path to a service account JSON file:
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Option B — inline credentials (use if running in an environment without file access):
# GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
# GOOGLE_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
